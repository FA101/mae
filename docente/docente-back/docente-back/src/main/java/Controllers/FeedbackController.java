package Controllers;

public class FeedbackController {

    //id, discenteAvaliador, disciplinaAvaliada, data, didatica(nota), metododeensino(nota),
    // esclarecimentoduvias(notas), ritmo(nota), comentario(texto 800)

}
